- project:
    check:
      jobs:
        - ansible-galaxy_ng-build-container-image
    pre-rlease:
      jobs:
        - release-ansible-python
    release:
      jobs:
        - release-ansible-python
